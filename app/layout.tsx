import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Onde Ir - Descubra lugares com IA",
  description: "Encontre os melhores lugares usando inteligência artificial. Digite o que você está procurando e deixe a IA encontrar os lugares perfeitos para você.",
  keywords: ["lugares", "busca", "IA", "inteligência artificial", "localização", "restaurantes", "cafés", "entretenimento"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
